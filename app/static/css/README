# app/static/css/

CSS stylesheets for the site.

## Guidelines
- Keep global/base styles in a main file (e.g. `main.css`)
- If the project grows, add page- or feature-specific files (e.g. `dashboard.css`)
- Prefer consistent naming and avoid inline styles in templates

## What belongs here
- CSS only
