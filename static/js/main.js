// Import the userPromo module
import { initUserPromos } from './userPromo.js';

// Initialize user promos on document ready
document.addEventListener('DOMContentLoaded', () => {
    initUserPromos();
});